# Orquestacion del grafo ciclico
langgraph
langgraph-checkpoint-sqlite   # AsyncSqliteSaver
aiosqlite                     # backend async de SQLite

# Herramientas (@tool) y tipos de mensaje
langchain
langchain-core

# LLM: instalar solo el del proveedor que se use (ver LLM_PROVIDER en .env.example)
langchain-google-genai        # LLM_PROVIDER=gemini  (por defecto)
# langchain-openai            # LLM_PROVIDER=openai
# langchain-anthropic         # LLM_PROVIDER=anthropic
# langchain-ollama            # LLM_PROVIDER=ollama  (local, sin API key)

# Utilidades
pydantic
python-dotenv
